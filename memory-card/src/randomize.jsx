/*
import { useState } from "react";

const Randomize = () => {
    const [shuffle, setShuffle] = useState(0);
    const [display, setDisplay] = useState(true);

    return(
        <>
        <div className="title">
            <h1>Memory Game</h1>
            <button onClick={() => {
            setShuffle(Math.floor(Math.random() * 100));
            setDisplay(true)}}>Play</button>
        </div>
        </>
    )
}

export default Randomize
*/